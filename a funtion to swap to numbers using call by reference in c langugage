#include <stdio.h>
#include <conio.h>

void swap(int *a, int *b)
{
    int temp;
    temp = *a;
    *a = *b;
    *b = temp;
}

int main()
{
    int x,y;

    printf("Before swap: %d,%d\n", x, y);

    swap(&x, &y);

    printf("After swap: %d %d\n", x, y);

    return 0;
}
